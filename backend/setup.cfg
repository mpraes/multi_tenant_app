# Packaging and tooling configuration for the backend framework.
[metadata]
name = multi-tenant-app
version = 0.1.0

[options]
package_dir =
    = src
packages = find:

[options.packages.find]
where = src
